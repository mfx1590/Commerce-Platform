I need a change outside my owned paths. Draft a GitHub issue titled "CONTRACT CHANGE: $ARGUMENTS" containing: the exact diff needed (file paths + before/after), why my mission cannot proceed without it, and the local mock I will use meanwhile. Create the issue with `gh issue create` labelled contract-change and window:<my key>, then add it under "Blocked / waiting" in my memory file.
