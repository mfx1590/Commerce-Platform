Update your memory file now: move finished tasks to Done with the commit sha (use the sha of the commit you are about to make, or "pending"), rewrite In progress with the exact next step and open files, rewrite Next, add any Decisions (with reasons) and Gotchas. Update the "Last updated / Last commit" line. Then stage the memory file together with the code changes and commit with a message of the form `<key>: <task> (memory updated)`. Finally tell me whether I should run /compact.
