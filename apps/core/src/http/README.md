# http — the contract HTTP layer

Owner: window 1 (core). Everything `src/server.ts` mounts **ahead of Medusa** through `mountCoreMiddleware(app)`:
request id, contract-header alias, the Store API tenant context, the Store API routes, the Admin API staff
principal, the Admin API routes, and the contract error renderer. Medusa's own routes and middleware come after
and only see what these did not answer.

## Files

| File                       | What                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `request-id.ts`            | `X-Request-Id` in (or generated) and out; lands in `audit_log.request_id`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `publishable-key-alias.ts` | copies `X-Publishable-Key` to Medusa's `x-publishable-api-key`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `tenant.ts`                | `storeContextMiddleware`: key → `store_api_key` → `req.tenant` (store-scoped client, default currency); 401                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `staff-auth.ts`            | `staffAuthMiddleware`: bearer token → `req.principal`. `KeycloakStaffTokenVerifier` (default: staff-realm JWT → `staff_user` → OpenFGA scope via hq-rbac's `createStaffScopeMiddleware`; principal carries `scope`), `DevTokenVerifier` (`CORE_DEV_TOKENS=1`, never in production; `role_assignment`), `composeStaffTokenVerifier` (the `dev:` routing rule); `storeClientFor` / `organizationClientFor` / `visibleStoresClientFor`                                                                                                                                                                                                                                           |
| `permissions.ts`           | `requirePermission(relation, objectFactory)` middleware, `can`, `ensurePermission` — OpenFGA through `@platform/auth-sdk` `can()` for principals with a `scope` (503 fail closed), the ADR 0002 stub over `role_assignment` (`hasPermission` / `assertPermission`) for dev-token principals                                                                                                                                                                                                                                                                                                                                                                                   |
| `hq-rbac-adapter.ts`       | Express adapter for `src/modules/hq-rbac` (window 2): `handle({ method, path, principal, scope, query, body, requestId })` with the resolved principal/scope; `null` → `next()`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `store-fallback.ts`        | `storeApiFallbackProxy(baseUrl)`: non-production verbatim proxy of every unhandled `/store/*` request to `CORE_STORE_API_FALLBACK_URL` (Prism mock); one log line per request, 502 when unreachable                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `openapi.ts`               | runtime loader of the frozen OpenAPI docs: `x-permission` per operation, request-body validation (400 `validation_error` with per-field `details`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `store-routes.ts`          | `GET /store`, `/store/categories`, `/store/products`, `/store/products/{handle}` (`?currency=` validated against the store's currencies) + cart: `POST /store/carts`, `GET`/`PATCH /store/carts/{cartId}`, line items add/update/remove (JSON bodies validated against store-api.yaml, `express.json` mounted on `/store/carts` only); checkout: `GET …/shipping-options`, `POST …/payment-session`, `POST …/complete` (`Idempotency-Key` ≥ 8 chars → 400 otherwise, 201 `Order`), `GET /store/orders/{orderId}` (customer bearer via `verifyCustomerToken` or `?email=`; 200/404 only, never logs the email or query); `REAL_STORE_PATHS` lists what the core answers itself |
| `admin-routes.ts`          | `/admin/me`, registry, catalog, orders (`listOrders` / `getOrder` / `cancelOrder`, 2.3) and inventory routes (`listInventoryLevels` — `store:{store_id}` or `store:*` when the query has no store_id —, `createStockMovement`, 2.4); each: `permission(op)` → `body(op)` → handler → module service                                                                                                                                                                                                                                                                                                                                                                           |
| `module-routers.ts`        | `moduleAdminRouters()`: Admin routers other windows' modules export, mounted after `adminRouter()` — the named mount point (window 9's `merchandisingRouter`, #162 part 3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `errors.ts`                | `coreErrorHandler` (contract `Error` shape; body-parser errors such as malformed JSON → 400 `validation_error`) and `handle()` for async handlers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `query.ts`                 | shared query/path parsing (`page`/`limit`, uuid params)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `types.ts`                 | Express `Request` augmentation: `req.tenant`, `req.principal`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## Rules

- Handlers never touch `pg`; they get a `ScopedClient` from `req.tenant.client`, `storeClientFor(principal, storeId)`,
  `organizationClientFor(principal)` or `visibleStoresClientFor(principal)` and pass it to a module service.
- Every Admin API route's permission and request schema come from `admin-api.yaml` itself — the route file only
  names the `operationId`.
- Errors are `AppError` (`src/lib/errors.ts`); anything else becomes `500 { code: "internal" }` without leaking
  details. No PII in logs.

## Tests

`test/tenant-http.test.ts` (RLS through HTTP, 401/403), `test/store-api.test.ts` and `test/admin-api.test.ts`
(contract replay, every response validated against the OpenAPI components via `test/helpers/openapi.ts`),
`test/publishable-key-alias.test.ts`, `test/guards.test.ts` (structural guards) — all with dev tokens.
`test/auth-live.test.ts` runs the same chain with real Keycloak tokens (test-cli password grant) and a throw-away
OpenFGA store (200 / 403 contract body / hq-rbac 200 for owner / 401 invalid JWT / 503 OpenFGA down), and skips
itself when Keycloak or OpenFGA is unreachable. `test/store-fallback.test.ts` proves the proxy against a local http
server. All run the exact chain `mountCoreMiddleware` mounts, on a bare Express app over a seeded throwaway database.
