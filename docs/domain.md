# Domain model (frozen with contracts-v0.1)

Expands plan section 3. Every window uses these names, verbatim, in SQL, OpenAPI, events, and UI.
Source of truth for the schema is `packages/db/migrations`; this document explains it and names the owner.

## Conventions

| Rule | Value |
|---|---|
| Identifiers | `uuid` primary keys generated by the database (`gen_random_uuid()`); the API exposes them unchanged |
| Tenancy | Every table has `organization_id uuid NOT NULL`. Store-scoped tables also have `store_id uuid NOT NULL`. RLS on both (ADR 0001) |
| Timestamps | `created_at timestamptz NOT NULL DEFAULT now()`, `updated_at timestamptz NOT NULL DEFAULT now()` on every mutable table |
| Money | integer minor units (`amount_minor bigint`) plus an ISO-4217 `currency char(3)`. Never floats |
| Codes | `code`/`handle`/`slug` are lowercase kebab-case, unique per store (or per organization for org-scoped tables) |
| Soft delete | none. Archive via `status`; hard deletes only through GDPR flows (window 13) |
| Audit | Every admin mutation writes `audit_log` (before/after JSON) in the same transaction |
| Events | Every state change on a starred entity in plan section 3 writes `outbox` in the same transaction (ADR 0003) |
| Naming | snake_case tables and columns, singular table names, `<table>_id` foreign keys |

Session context set by the tenant client (`packages/db`): `app.organization_id`, `app.scope` (`organization` | `store`),
`app.store_ids` (csv of uuids, used when scope is `store`), `app.actor_id`. Missing context means no rows.

## Ownership legend

Window numbers refer to `docs/memory/Memory-main.md` window index. "main" = main window (schema frozen, changes by CONTRACT CHANGE issue).
Owner means: owns the module code that mutates the entity; the schema itself is always `packages/db` (main).

---

## 1. Organization level (`organization_id` only)

### organization
HQ. Exactly one row in Phase 0–3 (`hq`); the model supports more.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| slug | text UNIQUE | `hq` |
| name | text | |
| default_currency | char(3) | `EUR` |
| settings | jsonb | free-form HQ settings |

Owner: window 1 (registry). Events: none.

### legal_entity
Accounting company. Every store belongs to exactly one (decision 7: one per brand).

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id | uuid FK organization | |
| code | text | unique per organization (`le-brand-a`) |
| name | text | registered name |
| country | char(2) | ISO-3166-1 |
| currency | char(3) | reporting currency |
| vat_number | text NULL | |
| odoo_company_id | int NULL | filled by window 15 |

Owner: window 1 (registry) for CRUD; window 15 (accounting) for `odoo_company_id`.

### warehouse
Physical stock location, shared by all stores (decision 3: own warehouses `wh-eu`, `wh-us`).

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id | uuid FK | |
| code | text | unique per organization |
| name | text | |
| address | jsonb | `Address` shape (below) |
| country | char(2) | |
| is_active | bool | |
| priority | int | allocation order, lower first |

Owner: window 11 (warehouse); Phase 1 CRUD by window 1. Events: none in v0.1.

### staff_user
Mirror of a Keycloak staff-realm user. Roles are NOT stored here (OpenFGA is the source of truth).

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id | uuid FK | |
| keycloak_subject | text UNIQUE | `sub` claim |
| email | text | unique per organization |
| display_name | text | |
| status | text | `active` / `disabled` |
| last_login_at | timestamptz NULL | |

Owner: window 2 (auth).

### role_assignment
Read mirror of OpenFGA tuples for listing in the admin UI. Written only by window 2's role API after the OpenFGA write succeeds.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id | uuid FK | |
| staff_user_id | uuid FK staff_user | |
| relation | text | one of the OpenFGA relations (ADR 0002): `owner`, `finance`, `operations`, `store_admin`, `store_staff`, `support`, `analyst` |
| object_type | text | `organization` / `store` |
| object_id | uuid | organization.id or store.id |
| UNIQUE | | (staff_user_id, relation, object_type, object_id) |

Owner: window 2 (auth).

### audit_log
Append-only. `store_id` nullable because organization-level actions exist.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id | uuid FK | |
| store_id | uuid NULL FK store | |
| actor_id | uuid NULL | staff_user.id, or NULL for system/customer |
| actor_type | text | `staff` / `customer` / `system` |
| action | text | `<entity>.<verb>` e.g. `product.update` |
| entity_type | text | table name |
| entity_id | uuid | |
| before | jsonb NULL | |
| after | jsonb NULL | |
| request_id | text NULL | |
| created_at | timestamptz | no updated_at |

Owner: window 2 (auth) for the writer helper; everyone calls it.

### outbox
Transactional outbox (ADR 0003). Schema lives in `packages/events/migrations`; listed here for completeness.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | event id, also the Kafka key for idempotency |
| organization_id | uuid | |
| store_id | uuid NULL | |
| topic | text | `order.placed` … (packages/events `EVENT_TOPICS`) |
| version | int | schema version |
| aggregate_type | text | `order`, `payment`, … |
| aggregate_id | uuid | |
| payload | jsonb | validated against the schema |
| headers | jsonb | trace/request ids |
| occurred_at | timestamptz | |
| published_at | timestamptz NULL | set by the relay |
| attempts | int | relay retries |

Owner: main (schema), window 14 (relay).

### customer_identity
Cross-brand identity link (plan 3: "identity may be linked across stores"). One row per person; `customer` rows point to it.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id | uuid FK | |
| email_hash | text | sha256(lowercased email), unique per organization |
| keycloak_subject | text NULL | customers realm `sub` |
| merged_into_id | uuid NULL FK customer_identity | after a merge |

Owner: window 13 (customers).

### ledger_entry
Derived by the accounting service from events only (Phase 4). Frozen now so the events carry everything it needs.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id | uuid FK | |
| legal_entity_id | uuid FK legal_entity | |
| store_id | uuid FK store | |
| journal | text | `sales`, `psp_fees`, `refunds`, `cogs`, `tax`, `shipping` |
| entry_date | date | |
| account_code | text | chart of accounts code |
| debit_minor | bigint | |
| credit_minor | bigint | |
| currency | char(3) | |
| source_event_id | uuid | outbox.id (unique with account_code) |
| source_topic | text | |
| reference_type / reference_id | text / uuid | order, payment, refund, shipment |
| posted_to_erp_at | timestamptz NULL | |
| erp_reference | text NULL | Odoo move id |

Owner: window 15 (accounting).

---

## 2. Store level (`organization_id` + `store_id`)

### store
A brand. `store_id` on every row below refers to this.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id | uuid FK | |
| legal_entity_id | uuid FK legal_entity | |
| code | text | unique per organization (`brand-a`) |
| name | text | |
| status | text | `draft` / `active` / `paused` / `archived` |
| default_currency | char(3) | must exist in store_currency |
| default_locale | text | BCP-47, must exist in store_locale |
| default_country | char(2) | |
| timezone | text | IANA |
| content_space_id | text NULL | Sanity dataset (decision 8) |
| search_index | text NULL | Algolia index name (decision 9) |
| psp_account_id | text NULL | Stripe Connect account (decision 10). Never a secret |
| theme | jsonb | design tokens override, consumed by `packages/ui` |
| settings | jsonb | |

Owner: window 1 (registry). Events: `store.created`, `store.updated`.

### store_domain

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| hostname | text UNIQUE (global) | `shop.brand-a.com` |
| is_primary | bool | exactly one primary per store |
| verified_at | timestamptz NULL | |

Owner: window 1 (registry).

### store_locale / store_currency

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| locale (BCP-47) / currency (char 3) | text | unique per store |
| is_default | bool | |

Owner: window 1 (registry).

### sales_channel

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| code | text | unique per store (`web`, `app`, `amazon`, `pos-1`) |
| name | text | |
| type | text | `web` / `app` / `marketplace` / `pos` |
| is_active | bool | |

Owner: window 1 (registry).

### store_api_key
Publishable key a storefront sends as `X-Publishable-Key`; resolves the store context (ADR 0001). Secret keys for server use share the table.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| name | text | |
| type | text | `publishable` / `secret` |
| key_prefix | text | first 8 chars, for display |
| key_hash | text UNIQUE | sha256; the plain key is shown once |
| sales_channel_id | uuid NULL FK | restrict a key to one channel |
| revoked_at | timestamptz NULL | |

Owner: window 1 (registry).

### product_category

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| parent_id | uuid NULL FK product_category | |
| handle | text | unique per store |
| name | text | |
| description | text NULL | |
| position | int | sort within parent |
| is_active | bool | |

Owner: window 1 (catalog).

### product

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| handle | text | unique per store |
| title | text | |
| subtitle | text NULL | |
| description | text NULL | markdown |
| status | text | `draft` / `published` / `archived` |
| category_id | uuid NULL FK product_category | primary category |
| brand_name | text NULL | manufacturer label (not the store) |
| tags | text[] | |
| attributes | jsonb | `{material: "cotton", …}` |
| seo | jsonb | title, description, canonical |
| thumbnail_url | text NULL | |
| published_at | timestamptz NULL | |

Owner: window 1 (catalog). Events: `product.published`, `product.updated`, `product.archived`.

### product_option
Option axis of a product (`Size`, `Color`).

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| product_id | uuid FK product | |
| name | text | unique per product |
| values | text[] | allowed values |
| position | int | |

Owner: window 1 (catalog).

### product_variant

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| product_id | uuid FK product | |
| sku | text | unique per store |
| barcode | text NULL | |
| title | text | `M / Red` |
| options | jsonb | `{Size: "M", Color: "Red"}` keys = product_option.name |
| manage_inventory | bool | false = infinite |
| allow_backorder | bool | |
| weight_g | int NULL | |
| dimensions_mm | jsonb NULL | `{l,w,h}` |
| hs_code | text NULL | customs |
| origin_country | char(2) NULL | |
| position | int | |

Owner: window 1 (catalog).

### product_media

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| product_id | uuid FK product | |
| variant_id | uuid NULL FK product_variant | |
| url | text | Cloudinary URL (window 9) |
| alt | text NULL | |
| position | int | |

Owner: window 1 (catalog); upload pipeline window 9.

### customer_group

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| code | text | unique per store |
| name | text | |

Owner: window 1 (pricing).

### price_list

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| code | text | unique per store; `default-eur` |
| name | text | |
| type | text | `default` / `sale` / `override` |
| currency | char(3) | |
| customer_group_id | uuid NULL FK | |
| sales_channel_id | uuid NULL FK | |
| starts_at / ends_at | timestamptz NULL | |
| status | text | `active` / `draft` / `expired` |
| priority | int | higher wins when several apply |

Owner: window 1 (pricing). Exactly one `default` list per (store, currency).

### price

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| price_list_id | uuid FK price_list | |
| variant_id | uuid FK product_variant | |
| currency | char(3) | equals price_list.currency |
| amount_minor | bigint | |
| compare_at_minor | bigint NULL | strike-through price |
| min_quantity | int | default 1 |
| UNIQUE | | (price_list_id, variant_id, min_quantity) |

Owner: window 1 (pricing).

### promotion

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| code | text NULL | coupon code, unique per store when set; NULL = automatic |
| name | text | |
| type | text | `percentage` / `fixed_amount` / `free_shipping` |
| value | int | basis points for percentage (1000 = 10%), minor units for fixed |
| currency | char(3) NULL | required for fixed_amount |
| rules | jsonb | `{min_subtotal_minor, product_ids, category_ids, customer_group_ids, sales_channel_ids, first_order_only}` |
| usage_limit | int NULL | |
| usage_count | int | |
| per_customer_limit | int NULL | |
| starts_at / ends_at | timestamptz NULL | |
| status | text | `active` / `draft` / `disabled` |

Owner: window 9 (promotions); Phase 1 CRUD by window 1. Events: none in v0.1.

### customer

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| identity_id | uuid NULL FK customer_identity | cross-brand link |
| keycloak_subject | text NULL | customers realm `sub`; NULL for guests |
| email | text | unique per store |
| first_name / last_name | text NULL | |
| phone | text NULL | E.164 |
| customer_group_id | uuid NULL FK | |
| status | text | `guest` / `registered` / `disabled` / `erased` |
| consent | jsonb | `{marketing_email: {granted, at, source}, …}` |
| metadata | jsonb | |

Owner: window 13 (customers); Phase 1 CRUD by window 1. Events: `customer.created`, `customer.updated`, `customer.erased`. PII: never logged, never in events beyond `customer_id` and hashed email.

### customer_address

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| customer_id | uuid FK customer | |
| Address fields | see `Address` | |
| is_default_shipping / is_default_billing | bool | |

Owner: window 13 (customers).

### tax_rate
Fallback table when no tax provider is configured (Phase 1). Window 7 replaces lookups with Stripe Tax.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| country | char(2) | |
| region | text NULL | state/province |
| name | text | `VAT 21%` |
| rate_bp | int | basis points |
| product_category_id | uuid NULL FK | category-specific rate |

Owner: window 7 (tax); Phase 1 by window 1.

### shipping_option

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| code | text | unique per store |
| name | text | shown at checkout |
| carrier | text | `manual`, `dhl`, `ups`, … |
| service | text NULL | carrier service code |
| price_minor | bigint | flat price in v0.1 |
| currency | char(3) | |
| countries | char(2)[] | eligible destinations |
| sales_channel_id | uuid NULL FK | |
| rules | jsonb | `{min_subtotal_minor, max_weight_g}` |
| is_active | bool | |

Owner: window 8 (shipping); Phase 1 by window 1.

### cart

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| sales_channel_id | uuid FK | |
| customer_id | uuid NULL FK | |
| email | text NULL | |
| currency | char(3) | |
| locale | text | |
| country | char(2) | shipping destination, drives tax/shipping |
| shipping_address / billing_address | jsonb NULL | `Address` |
| shipping_option_id | uuid NULL FK | |
| promotion_codes | text[] | |
| payment_session | jsonb NULL | `{provider, session_id, status}` — never card data |
| subtotal_minor / discount_minor / shipping_minor / tax_minor / total_minor | bigint | recomputed on every change |
| status | text | `active` / `completed` / `abandoned` |
| order_id | uuid NULL FK order | set on completion |
| completed_at | timestamptz NULL | |
| metadata | jsonb | |

Owner: window 1 (checkout). Events: none (carts are not accounting-relevant); `cart.abandoned` deferred to Phase 4.

### cart_line_item

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| cart_id | uuid FK cart | |
| variant_id | uuid FK product_variant | |
| sku / title / variant_title / thumbnail_url | text | snapshot |
| quantity | int | > 0 |
| unit_price_minor | bigint | snapshot at add time |
| discount_minor | bigint | allocated |
| tax_rate_bp | int | |
| metadata | jsonb | |
| UNIQUE | | (cart_id, variant_id) |

Owner: window 1 (checkout).

### order

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| display_id | bigint | per-store sequence, shown to humans (`#1042`) |
| sales_channel_id | uuid FK | |
| cart_id | uuid NULL FK | |
| customer_id | uuid NULL FK | |
| email | text | |
| currency | char(3) | |
| locale | text | |
| status | text | `pending` / `confirmed` / `processing` / `completed` / `cancelled` |
| payment_status | text | `awaiting` / `authorized` / `captured` / `partially_refunded` / `refunded` / `failed` |
| fulfillment_status | text | `unfulfilled` / `partially_fulfilled` / `fulfilled` / `partially_returned` / `returned` |
| shipping_address / billing_address | jsonb | `Address` snapshot |
| shipping_option_id | uuid NULL FK | |
| shipping_method | jsonb | snapshot `{code, name, carrier, price_minor}` |
| promotion_codes | text[] | |
| subtotal_minor / discount_minor / shipping_minor / tax_minor / total_minor | bigint | frozen at placement |
| placed_at | timestamptz | |
| cancelled_at / completed_at | timestamptz NULL | |
| cancel_reason | text NULL | |
| metadata | jsonb | |
| UNIQUE | | (store_id, display_id) |

Owner: window 1 (orders). Events: `order.placed`, `order.confirmed`, `order.cancelled`, `order.completed`, `order.updated`.

### order_line_item

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| order_id | uuid FK order | |
| variant_id | uuid NULL FK | NULL if variant later deleted |
| sku / title / variant_title / thumbnail_url | text | snapshot |
| quantity | int | |
| unit_price_minor | bigint | |
| discount_minor | bigint | |
| tax_rate_bp | int | |
| tax_minor | bigint | |
| total_minor | bigint | quantity*unit − discount + tax |
| fulfilled_quantity / returned_quantity | int | |
| metadata | jsonb | |

Owner: window 1 (orders).

### payment

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| order_id | uuid FK order | |
| provider | text | `stripe`, `manual` |
| provider_payment_id | text NULL | unique per provider |
| amount_minor | bigint | |
| currency | char(3) | |
| status | text | `pending` / `authorized` / `captured` / `failed` / `cancelled` |
| fee_minor | bigint NULL | PSP fee once known (payout) |
| authorized_at / captured_at | timestamptz NULL | |
| failure_reason | text NULL | |
| idempotency_key | text UNIQUE | |
| metadata | jsonb | never card data |

Owner: window 7 (payments); Phase 1 `manual` provider by window 1. Events: `payment.authorized`, `payment.captured`, `payment.failed`.

### refund

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| order_id | uuid FK order | |
| payment_id | uuid FK payment | |
| return_id | uuid NULL FK return | |
| amount_minor | bigint | |
| currency | char(3) | |
| reason | text | `return` / `cancellation` / `goodwill` / `chargeback` |
| status | text | `pending` / `succeeded` / `failed` |
| provider_refund_id | text NULL | |
| requested_by | uuid NULL | staff_user.id |
| idempotency_key | text UNIQUE | |

Owner: window 7 (payments). Events: `refund.issued`, `refund.failed`.

### shipment

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| order_id | uuid FK order | |
| warehouse_id | uuid FK warehouse | |
| carrier | text | |
| service | text NULL | |
| tracking_number | text NULL | |
| tracking_url | text NULL | |
| label_url | text NULL | |
| cost_minor | bigint NULL | what we pay the carrier |
| currency | char(3) | |
| status | text | `pending` / `label_created` / `shipped` / `in_transit` / `delivered` / `failed` / `cancelled` |
| shipped_at / delivered_at | timestamptz NULL | |
| metadata | jsonb | |

Owner: window 8 (shipping). Events: `shipment.created`, `shipment.shipped`, `shipment.delivered`.

### shipment_item

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| shipment_id | uuid FK shipment | |
| order_line_item_id | uuid FK order_line_item | |
| quantity | int | |

Owner: window 8 (shipping).

### return

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| order_id | uuid FK order | |
| status | text | `requested` / `approved` / `received` / `refunded` / `rejected` |
| reason | text NULL | |
| warehouse_id | uuid NULL FK | where it is received |
| refund_id | uuid NULL FK refund | |
| requested_at / received_at | timestamptz | |
| metadata | jsonb | |

Owner: window 1 (orders) Phase 2; support flows window 16. Events: `return.requested`, `return.received`.

### return_item

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| return_id | uuid FK return | |
| order_line_item_id | uuid FK | |
| quantity | int | |
| condition | text NULL | `resellable` / `damaged` |

Owner: window 1 (orders).

### inventory_level
Stock of one variant in one warehouse. `store_id` = the variant's store (variants are store-owned; warehouses are shared).

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| variant_id | uuid FK product_variant | |
| warehouse_id | uuid FK warehouse | |
| on_hand | int | physical |
| reserved | int | held by unshipped orders |
| incoming | int | purchase orders |
| available | int GENERATED | on_hand − reserved |
| UNIQUE | | (variant_id, warehouse_id) |

Owner: window 1 (inventory); allocation policy window 11. Events: `stock.moved` (from stock_movement).

### stock_movement
Append-only ledger of stock changes; `inventory_level` is its projection.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| variant_id | uuid FK | |
| warehouse_id | uuid FK | |
| delta | int | signed |
| reason | text | `receipt` / `sale` / `return` / `adjustment` / `transfer_in` / `transfer_out` / `cycle_count` |
| reference_type | text NULL | `order`, `shipment`, `return`, `transfer` |
| reference_id | uuid NULL | |
| actor_id | uuid NULL | |
| note | text NULL | |
| created_at | timestamptz | no updated_at |

Owner: window 1 (inventory). Events: `stock.moved`.

### reservation
Stock held for an order (or a cart during checkout) until shipped or expired.

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| variant_id | uuid FK | |
| warehouse_id | uuid FK | |
| order_id | uuid NULL FK | |
| cart_id | uuid NULL FK | |
| quantity | int | |
| expires_at | timestamptz NULL | carts only |
| released_at | timestamptz NULL | |

Owner: window 1 (inventory).

---

## 2b. Marketing (store level unless noted; migration 0120, contracts-v0.3)

Spec: docs/marketing-scope.md. Marketing reads events and writes its own tables; no campaign ever mutates an order.
Module owner for all of them is window 17 (marketing) except where noted.

### campaign

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| name | text | |
| type | text | `email` / `sms` / `paid_social` / `paid_search` / `affiliate` / `referral` / `landing` |
| status | text | `draft` / `scheduled` / `active` / `paused` / `ended` |
| starts_at / ends_at | timestamptz NULL | `ends_at >= starts_at` |
| budget_minor | bigint NULL | requires `currency` when set |
| currency | char(3) NULL | |
| utm_source / utm_medium / utm_campaign | text NULL | `utm_campaign` links `attribution` rows to the campaign |
| promotion_id | uuid NULL FK promotion | |
| segment_id | uuid NULL FK segment | |
| landing_path | text NULL | |
| external_ref | text NULL | Klaviyo flow id, Meta campaign id |
| launched_at / ended_at | timestamptz NULL | |
| metadata | jsonb | |

Owner: window 17. Events: `campaign.launched`, `campaign.ended`.

### segment (organization level with optional store)

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id | uuid | |
| store_id | uuid NULL FK store | NULL = organization template, visible only in organization scope (RLS `store_nullable`) |
| template_id | uuid NULL FK segment | the template a store segment was copied from; NULL for templates |
| name | text | unique per store; templates unique per organization |
| description | text NULL | |
| rules | jsonb | `{orders_count, last_order_at, total_spent_minor, tags, consent, country, customer_group_ids, …}`; grammar frozen by window 17 in Phase 2.3 |
| materialised_count | int | refreshed by the materialise job |
| last_materialised_at | timestamptz NULL | |

Owner: window 17. Events: none.

### segment_member

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| segment_id | uuid FK segment | cascade |
| customer_id | uuid FK customer | cascade |
| materialised_at | timestamptz | |
| UNIQUE | | (segment_id, customer_id) |

Owner: window 17 (materialise job). Events: none. No `updated_at`: rows are replaced, not edited.

### product_feed

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| name | text | |
| channel | text | `google_merchant` / `meta` / `tiktok` / `pinterest` |
| locale | text | |
| currency | char(3) | one of the store's currencies |
| filters | jsonb | `{category_ids, tags, in_stock_only, …}` |
| mapping | jsonb | channel attribute → product field overrides |
| url | text NULL | public URL once published |
| status | text | `draft` / `active` / `paused` / `error` |
| last_published_at | timestamptz NULL | |
| item_count | int | |
| errors | jsonb | `[{code, message, product_id?}]` |

Owner: window 17 (apps/feeds). Events: `feed.published`. Feed items are computed, not stored.

### attribution

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| order_id | uuid FK order | cascade |
| cart_id | uuid NULL FK cart | |
| touch | text | `first` / `last` |
| utm_source / utm_medium / utm_campaign / utm_term / utm_content | text NULL | |
| referrer | text NULL | origin only (scheme + host), never a full URL |
| landing_path | text NULL | |
| campaign_id | uuid NULL FK campaign | matched on `utm_campaign` at placement |
| captured_at | timestamptz | when the storefront captured the touch |
| created_at | timestamptz | when the core recorded it (order placement); no `updated_at`, rows are immutable |
| UNIQUE | | (order_id, touch) |

Owner: written by window 1 (core) at order placement from `cart.metadata.attribution`, read by window 17. Events: `attribution.recorded` (one per touch, same transaction as `order.placed`).

### referral_program

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| name | text | |
| status | text | `draft` / `active` / `paused` / `ended` |
| referrer_reward_promotion_id / referee_reward_promotion_id | uuid NULL FK promotion | |
| rules | jsonb | `{min_order_minor, reward_after, max_rewards_per_referrer, …}` |

Owner: window 17. Events: none.

### referral

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| program_id | uuid FK referral_program | cascade |
| referrer_customer_id | uuid FK customer | |
| code | text | unique per store; shared as `/r/{code}`; events carry only `sha256(code)` |
| referee_customer_id | uuid NULL FK customer | |
| order_id | uuid NULL FK order | |
| status | text | `created` / `clicked` / `converted` / `rewarded` |
| clicked_at / converted_at / rewarded_at | timestamptz NULL | |

Owner: window 17. Events: `referral.converted`.

### review

| Field | Type | Notes |
|---|---|---|
| id | uuid PK | |
| organization_id / store_id | uuid | |
| product_id | uuid FK product | cascade |
| order_line_item_id | uuid NULL FK order_line_item | at most one review per line item |
| customer_id | uuid NULL FK customer | |
| rating | int | CHECK 1–5 |
| title / body | text NULL | customer text: API only, never in events |
| status | text | `pending` / `published` / `rejected` |
| moderated_by | uuid NULL FK staff_user | |
| moderated_at | timestamptz NULL | |
| moderation_reason | text NULL | staff-only |
| published_at | timestamptz NULL | |

Owner: window 17. Events: `review.published`.

### abandoned carts (no table)

Derived from `cart` with `status = 'active'` and no activity for the store's abandonment window; the core job flips
`cart.status` to `abandoned` and emits `cart.abandoned` (window 1); window 17 and window 16 consume it.

---

## 3. Shared value objects (JSON, not tables)

### Address
```json
{
  "first_name": "string", "last_name": "string", "company": "string|null",
  "line1": "string", "line2": "string|null", "city": "string", "region": "string|null",
  "postal_code": "string", "country": "NL", "phone": "string|null"
}
```

### Money
`{ "amount_minor": 1999, "currency": "EUR" }` — the API always returns money this way.

---

## 4. Relationship map

```
organization 1─* legal_entity 1─* store
organization 1─* warehouse
organization 1─* staff_user 1─* role_assignment
organization 1─* customer_identity 1─* customer
store 1─* store_domain | store_locale | store_currency | sales_channel | store_api_key
store 1─* product_category (tree) ; product *─1 product_category
store 1─* product 1─* product_option ; product 1─* product_variant 1─* price ; product 1─* product_media
store 1─* price_list 1─* price ; price_list *─? customer_group ; price_list *─? sales_channel
store 1─* promotion ; store 1─* tax_rate ; store 1─* shipping_option
store 1─* customer 1─* customer_address ; customer *─? customer_group
cart 1─* cart_line_item ; cart ─1 order (on completion)
order 1─* order_line_item ; order 1─* payment 1─* refund ; order 1─* shipment 1─* shipment_item ; order 1─* return 1─* return_item
product_variant *─* warehouse via inventory_level ; stock_movement / reservation reference both
every mutation ─► outbox ─► bus ─► ledger_entry (Phase 4)
```

## 5. Entity → window ownership summary

| Window | Entities (module code) |
|---|---|
| 1 core | organization, legal_entity (CRUD), store, store_domain, store_locale, store_currency, sales_channel, store_api_key, product_category, product, product_option, product_variant, product_media, customer_group, price_list, price, cart, cart_line_item, order, order_line_item, return, return_item, inventory_level, stock_movement, reservation; Phase 1 stand-ins for promotion, tax_rate, shipping_option, customer, payment(manual) |
| 2 auth | staff_user, role_assignment, audit_log helper |
| 7 payments | payment, refund, tax_rate (Stripe Tax) |
| 8 shipping | shipment, shipment_item, shipping_option |
| 9 search | promotion, product_media pipeline, search index sync |
| 11 warehouse | warehouse, allocation policy over inventory_level |
| 13 customers | customer, customer_address, customer_identity |
| 14 events | outbox relay |
| 15 accounting | ledger_entry, legal_entity.odoo_company_id |
| 17 marketing | campaign, segment (+ templates), segment_member, product_feed, referral_program, referral, review; reads attribution |
| 1 core (marketing) | attribution (written at order placement from cart.metadata.attribution), `cart.abandoned` job |
| main | the schema itself (`packages/db`), this document |
