# Memory main — whole-project state
Operating guide (how to open, resume, review, close windows): docs/HOW-I-RUN-THIS.md
Owner: main window (Architect in Phase 0, Integrator in every integration period).
Last updated: 2026-09-08 · Current phase: Integration 1 in review → Phase 2 · Contracts tag: contracts-v0.4 = fa09cd2 (Admin API 0.4.0, db 0.2.1; v0.3 583b407, v0.2 68f67a4, v0.1 1293ae7) · main last commit: see Current status

## What this project is
A multi-brand commerce platform: one HQ control plane, one multi-tenant commerce core (Medusa 2, Postgres RLS on store_id),
N brand storefronts (Next.js), one admin app with permission-driven HQ/Store views, event bus → automatic accounting, CRM, BI.
Full design: docs/plan/multi-brand-commerce-master-plan.md · Diagrams: docs/plan/architecture.svg, docs/plan/build_phases.svg
How to run the project solo on Max 5x: docs/plan/solo-max5x-schedule.md · Owner tasks + kickoff messages: docs/plan/owner-playbook-and-window-kickoffs.md

## Fixed decisions (edit docs/decisions.md, then mirror here)
- Language: TypeScript everywhere · Core: Medusa 2 · Storefront/Admin: Next.js App Router · Auth: Keycloak + OpenFGA
- Cloud: AWS · Managed-first for Phases 0–3 (Vercel, Neon, Upstash, Redpanda Cloud)
- Markets/currencies: A=EU/EUR, B=UK/GBP, C=US/USD · Warehouses: own (wh-eu, wh-us) · Accounting target: Odoo · Legal entities: one per brand
- CMS: Sanity (dataset per brand) · Search: Algolia (index per brand) · PSP: Stripe (Connect), no local PSP before Phase 3
- Solo operator on Claude Max 5x: ONE build window active at a time, Reviewer session for merges, strongest model only for main window + events/accounting

## Current status
- **2026-09-08 late: contracts-v0.4 tagged on fa09cd2** (#185 merged: Admin API 0.4.0 merchandising rules #162 + 401/403 on every operation #180, migration 0130; #162/#180 closed). Windows merge main and get the real migration; window 9's follow-up is docs only. #184 admin 2.1 refused by the queue on its own contract test that asserted the #180 gap ("no documented refusal" → now Prism answers 403) — window 4 flips the test and re-pushes, then #184 re-queues.
- **Merge rounds 9–10 (2026-09-08):** merged #182 marketing 2.1 (7c7ad01), #177 cms 2.2 (947300a — **wave C opens**: windows 10 brands `../wt-brands`, 3 storefront `../wt-storefront` on storefront/phase2), #174 core 2.3 (0e46031; orders state machine, `markPayment*`/`markShipment*` wrappers, `PaymentProvider.void`, #159 part 2, #162 mount point). Approved, queued next: #184 admin 2.1 (needs main merged for the allowlisted scan). #185 contracts 0.4.0 (#162 + #180, migration 0130) refused once on a runner-level failure (three jobs failed in 2 s with no step recorded) — rerun, then queue again; tag `contracts-v0.4` on its merge. gitleaks incident: window 7's test literals reddened every PR (scan covers all branches); anchored allowlist on main b194266. **Admin design: the owner's Medusa-rail idea** → docs/admin-design.md + docs/design/* (580331e), prototype https://claude.ai/code/artifact/52e5ceaa-18fb-4519-88e7-e59a312e7696, issue #192 for window 4 before 2.2. Decisions: marketing feeds = core renders + hashes via a FeedStorage seam, apps/feeds serves (`<store_code>/<feed_id>.<ext>`); price lists under promotions; #176/#179/#181 wiring batch in core 2.4; #178/#169 → window 3 wave C; window 7 files the shared webhook_event CONTRACT CHANGE with window 8's requirements.
- **Merge round 8 (2026-09-08):** merged #171 admin #154 E2E_CHANNEL (3ba4d0f), #166 search 2.2 merchandising rules (4b14a8a; router exported, mounted by core; `MemoryRulesRepository` until 0130 lands), #175 shipping 2.1 carrier provider (2468783; manual + EasyPost over fetch, test keys only). #174 core 2.3 (orders state machine, wrappers for 7/8, `PaymentProvider.void`) reviewed MERGE, in the queue. Root config applied: #167 storefront depends on `@platform/cms`, #170 cms owns `test/cms-*`, #173 EASYPOST_*, STRIPE_* rows (b9baf84). Contract changes accepted: #162 (lands as Admin API 0.4.0 via branch `main/contracts-0.4` → PR right after #174; the manager's commit also removes the search tests' proposed-DDL application so main stays green), #168 (after search 2.3), #172 (after core 2.4). Decisions: price lists live under `apps/core/src/modules/promotions/` (window 9); search module writes `product_media`/`product.thumbnail_url` for Phase 2 and requests catalog public media functions from window 1; window 7 builds capture against core 2.3's `markPayment*` wrappers, never writes `payment_status` directly; windows 7, 8, 17 file ONE REQUEST to window 1 for boot registration + mount lines; marketing report aggregates in the store default currency (no `mixed_currency` flag until Int 2). Incident: two Claude sessions opened in `../wt-admin` at once (owner double-open); the original session keeps 2.1, the peer must be stopped — one session per folder, always.
- **Merge round 7 (2026-09-08): #165 core 2.2 merged (721854f; checkout completion — shipping options, payment session behind `PaymentProvider` with a `manual` provider, placement with idempotency on `payment.idempotency_key`, display id via the store trigger, attribution rows + `attribution.recorded` + `order.placed` in one transaction, order read 200/404; closes #104). WAVE B OPENS:** windows 7 payments (`../wt-payments`, payments/phase2, Fable), 8 shipping (`../wt-shipping`, shipping/phase2, Sonnet), 17 marketing (`../wt-marketing`, marketing/phase2, Fable), 4 admin (`../wt-admin` switched to admin/phase2, Opus). Interfaces they build against: `apps/core/src/modules/cart/index.ts` (`TaxCalculator`/`setTaxCalculator`, `ShippingRateProvider`/`setShippingRateProvider`), `apps/core/src/modules/checkout/index.ts` (`PaymentProvider`/`setPaymentProvider`, `manualPaymentProvider`; `void`/`cancel` added in core 2.3). Follow-ups in core 2.3: order read 404 for malformed id/email, PaymentProvider void, #159 part 2, #162 router mount. Wave C (10, 3) opens when cms 2.2 merges. Infra guard note (window 5): `infra/ci/check-image-manifests.sh` fails the build as soon as `apps/feeds` or `apps/storefronts/brand-a` exist until every Dockerfile's deps stage lists them — windows 17 and 10 file the REQUEST to window 5 with their first PR.
- **Merge round 6 (2026-09-08):** merged #163 cms 2.1 (a58c3da; `@platform/cms` 0.1.0, one dataset per brand = store.code, locale-per-document decision; closes #119). Lockfile gained the `cms` importer; main reinstalled. Decisions: #164 accepted (cms owns `apps/storefront-starter/src/app/api/cms/**`, ownership.md 755e29a); #162 accepted (Admin API 0.4.0 merchandising rules + migration 0130, lands right after the search 2.2 PR merges; window 9 then removes its `proposed/` copies). In review: #165 core 2.2 checkout completion (Fable reviewer). Wave B opens when #165 merges.
- **Merge round 5 (2026-09-08):** merged #160 search 2.1 (f2dbf54; Algolia index per store, outbox-polling incremental sync, closes #134) and #161 infra close-out (ae7534a) — **window 5's Phase 2 is complete; the window is quiet until reopened by REQUEST** (signalled: storefront-against-core CI variant after core 2.2, images for apps/feeds and brand-a, Lighthouse job for window 3). Decisions: search 2.2 stores merchandising rules in a new `merchandising_rule` table (additive CONTRACT CHANGE expected from window 9: migration 0130 + Admin API `/admin/stores/{storeId}/merchandising/rules`; the manager lands it after the next open PR merges); index name = `store.search_index` (seed `<code>_products`), #134's `products_<code>` was only the default. #159 part 1 on main (f9d7767), part 2 → core 2.3 PR. Root config for cms on main (94f1de3, #158). In flight: core 2.2 (#104) PR about to open (approved plan: idempotency on payment.idempotency_key, display id via the store-row trigger; storefront cannot render the seeded catalogue against the core because next/image rejects picsum thumbnails → window 3's #109); cms 2.1 (#119) building; search 2.2 (#135) building against a local mock.
- **Merge round 4 (2026-09-08, after Int 1):** merged #156 infra 2.6 (6931293; closes #36, #99 — window 5's Phase 2 is complete, window goes quiet until REQUESTs) and #157 core 2.1 (7adcdd9; cart module + `currency` on product reads, closes #103). Decisions accepted from window 1: carts bypass Medusa's cart module and live in our `cart`/`cart_line_item` tables under RLS (closes the 1.8 mirror question: no Medusa mirror of stores/keys); tax and shipping pricing sit behind `TaxCalculator` / `ShippingRateProvider` exported from `apps/core/src/modules/cart/index.ts` with table defaults — windows 7 (#127) and 8 (#130) call `setTaxCalculator` / `setShippingRateProvider` at boot, never edit the module. ADR 0006 secret management on main (d5a003b, REQUEST #155). #154 (E2E_CHANNEL in admin Playwright) → window 4 wave B; window 3 part → #109. Core 2.2 (#104) plan approved (idempotency on `payment.idempotency_key`, display id via the store-row trigger, 402/409/404 per contract). Wave A open: windows 1 (core/phase2), 9 (search/phase2, `../wt-search`), 6 (cms/phase2, `../wt-cms`). Wave B (7, 8, 17, 4) opens when core #104 merges.
- **2026-09-08: INTEGRATION 1 DONE — #152 (c5c5e00) + follow-up #153 (583b407) merged; `contracts-v0.3` tagged on 583b407 and pushed.** Phase 2 wave A opens now: window 1 core (`./scripts/new-window.sh 1 2`, paste docs/start-messages/00-resume-any-window.md with n=1/core), window 9 search (`new-window.sh 9 2`, 09-search.md), window 6 cms (`new-window.sh 6 2`, 06-cms.md); window 5 continues with 2.6 (#36). Wave B (7, 8, 17, 4) after core 2.1–2.2 merge; wave C (10, 3) after cms 2.2 + core 2.2. The `../wt-integration` worktree stays until Int 2 (branch integration/phase1 is done).
- **2026-09-08: INTEGRATION 1 MERGED (#152 → main c5c5e00) — with one gap, fixed in the follow-up PR.** The owner asked, before the merge, for an explicit `CORE_STORE_API_FALLBACK=1` opt-in with production-first refusal and a plain real/mocked statement in the report. The manager stopped the running merge queue to add them, but stopping the wrapper did not kill `merge-queue.sh` itself: it pushed its merge commit and merged #152 with the earlier (URL-only, still production-refusing) version. The flag commit is landing as the follow-up PR from `integration/phase1`; **contracts-v0.3 is tagged on that follow-up's merge commit**, and Phase 2 windows open only after it. Lesson recorded in Global gotchas and MANAGER-HANDOFF §3.
- **2026-09-08: INTEGRATION 1 in PR (branch `integration/phase1`, worktree `../wt-integration`).** Delivered: contracts-v0.3 (Admin API 0.3.0 marketing paths, Store API 0.3.0 `currency`, events 0.2.0 with 7 new topics, db 0.2.0 migration 0120), real Keycloak staff tokens + OpenFGA permissions + hq-rbac mounted in the core, `ADMIN_API_URL` switch in the admin, `apps/core/src/lib/attribution.ts` for placement, `CORE_STORE_API_FALLBACK_URL` proxy, one checkout driven end to end through the core (report below). Phase 2 issues #103–#150 created; memory files 1, 3, 4, 6, 7, 8, 9, 10, 17 rewritten. **Next action (manager):** review the integration PR with a reviewer agent, merge it through `scripts/merge-queue.sh`, tag `contracts-v0.3`, then open Phase 2 wave A (windows 1, 9, 6; 5 continues 2.5/2.6) per the Phase 2 window plan — paste messages docs/start-messages/<nn>-<key>.md, worktrees via `./scripts/new-window.sh <n> 2` (it now creates the phase branch inside an existing worktree). Window 5 note: it recreated the whole docker stack during 2.5 (observability profile) while the integrator was running tests — remind it of the shared-stack rule; the manager restored the stack with `pnpm dev`.
- **Phase 0 done — tag contracts-v0.1.** Owner runs `git tag contracts-v0.1 && git push --tags` on main (commit listed below), then the next action.
- **Operating mode changed 2026-09-04: PARALLEL.** This window is the MANAGER (reviews, merges, integrates, contracts, Memory-main); it builds no features. Five build windows run at once in worktrees: `../wt-core` (core/phase1), `../wt-auth` (auth/phase1), `../wt-storefront` (storefront/phase1), `../wt-admin` (admin/phase1), `../wt-infra` (infra/phase2, Phase 2 tasks started early; cloud-account tasks stay at validate/plan until credentials exist). Procedure: docs/HOW-I-RUN-THIS.md section 1b.
- **Next action (owner):** open five terminals, `cd ../wt-<key> && pnpm install && claude`, paste the window's start message. Issues: window:core #1–9, window:auth #10–16, window:storefront #17–23, window:admin #24–30, window:infra #31–36.
- **Merge round 1 (2026-09-05):** merged #38 auth 1.1+1.2 · #49 core 1.1 · #42 admin 1.1–1.4 · #39 storefront 1.1–1.2 (all merge commits; lockfile regenerated by the manager on each branch). main green: 16 packages, 294 tests. Also merged: #45 infra 2.1 — it went in with a red `images` check (manager script bug, fixed): the core Docker image cannot run `medusa build` (dev deps + medusa-config missing in the build stage) → issue #59 for window 5; the `images` job is `continue-on-error` on main until #59 lands. Reviewed-not-yet-merged: core 1.2–1.6 (approved from t-branches; land via the folded core/phase1). Open follow-ups: #43 (window 2, OTP conditional in dev realm), #48 (window 1, deps in apps/core), core dev-token verifier must become explicit opt-in before 1.7 lands, admin: DEV_SESSION_SECRET removal + callback/middleware tests. Tags pushed: contracts-v0.1 (1293ae7), contracts-v0.2 (68f67a4).
- **Marketing domain added 2026-09-05** (owner decision, scope by the manager): docs/marketing-scope.md · window 17 (`marketing/`) starts Phase 2 after Integration 1 delivers tables, events and Admin API marketing paths as contracts-v0.3 · Phase 1 pre-work issued: #62 (window 3: UTM/referrer capture into cart.metadata.attribution), #63 (window 4: Marketing navigation placeholder). `cart.abandoned` moves from Phase 4 to Phase 2 (window 1 emits).
- **2026-09-08 handoff:** the manager window hit its context limit after Phase 1. Everything it knew is in docs/MANAGER-HANDOFF.md (runbook) and this file. Manager tooling moved into the repo: `scripts/merge-queue.sh`, `scripts/status.sh`. **Next manager session: open the repo root, paste docs/start-messages/00-manager-resume.md** (it runs Integration 1 and manages window 5 meanwhile). Open now: #98 infra 2.4b (approved, merging), window 5 starts 2.5 after it; follow-ups #90, #99, #102.
- **2026-09-08: PHASE 1 COMPLETE.** All four windows delivered: core 1.1–1.9 + sort/order, auth 1.1–1.7 (gate green in CI against real Keycloak/OpenFGA), storefront 1.1–1.8, admin 1.1–1.8. Contracts now: Store API 0.2.0, Admin API 0.2.1, CONTRACTS_VERSION 0.2.2 (accepted changes #56 sort/order, #77 customers=support, #100 cart/order metadata). db at migration 0110. Infra (Phase 2) has 2.1–2.4 merged, #98 in review, 2.5/2.6 to come. **Next: Integration 1** — open integration/phase1, paste docs/start-messages/00-integrator.md. Open follow-ups: #90 (auth finance ping), #99 (infra browser-channel grep), #102 (storefront cookie cap + catch), and the (account)/(content) English-only gap for windows 13 and 6.
- **2026-09-07 late:** core Phase 1 COMPLETE (#91 sort/order + analyst-PII gate merged; 1.1–1.9 + follow-up done). Infra #87 measured: auth-e2e 3m35s (62 live auth tests + 6 Playwright), images 8m5s and no longer triggered by app-source PRs (trade: an app change breaking its own image is caught at merge, not on the PR). Remaining Phase 1: storefront 1.6/1.7/1.8, admin 1.7/1.8. **Integration 1 is unblocked** — main window opens integration/phase1 when the owner says go.
- **2026-09-07 evening:** auth Phase 1 COMPLETE (#85 1.5, #88 1.6, #89 1.7 gate merged; live CI job from #87 green → gate "store admin cannot open Finance / analyst no PII" proven). Infra: #87 (live auth + e2e CI job, images only on image-defining changes on PRs) merged; 2.3 next. GitHub Actions budget set to $15/month by the owner after minutes ran out. Follow-up #90 (finance ping removal, exhaustive sweep). Left in Phase 1: core sort/order PR; storefront 1.6, 1.7, 1.8; admin 1.7, 1.8. Integration 1 can start once core's last PR lands (storefront/admin remaining tasks are mock-only and can finish in parallel with integration).
- **Merge round 3 (2026-09-07):** merged #74 infra 2.2 Terraform, #76 storefront 1.5, #81 infra 2.4a caching, #75 auth 1.4, #78 admin 1.6; #79 core 1.9 in queue. Contracts: Admin API 0.2.1 (#77, customers read = support). Decisions: #80 (CI job: Keycloak+OpenFGA via compose, live auth suites + Playwright journeys) → window 5 before 2.3, required before auth 1.7; #82 → window 2 in 1.5. Phase 1 left: core sort/order follow-up · auth 1.5–1.7 · storefront 1.6, 1.7, 1.8(#62) · admin 1.7, 1.8(#63). Infra next: #80, then 2.3 Helm/ArgoCD (go-ahead given), 2.4b, 2.5, 2.6.
- **Merge round 2 (2026-09-06):** #61 core 1.2–1.7 merged (guard fix verified). #69 infra (images fix, job required again) and #65 auth (OTP conditional) merged. #66 storefront 1.4 merged after the 409 `available` fix. Merged: #67 admin 1.5, #71 core 1.8 (verifier + ts-node, closes #60), #73 auth 1.3. Phase 1 progress: core 8/9 (1.9 + sort follow-up left), auth 3/7 (1.4–1.7), storefront 4/7 (1.5–1.7 + 1.8 UTM), admin 5/7 (1.6, 1.7 + 1.8 nav), infra 2.1 done, 2.2 parked → PR next. Follow-ups noted on the PRs: admin media positions renumbering (1.6); auth README mapping paragraph + memory tidy (1.4). #72 (.medusa ignore) applied. Main CI fully green incl. images since #69. Decisions sent: #60 accept (window 1, ts-node devDependency), #68 accept (window 3, next.config.mjs), window 4 starts 1.6 locally, window 5 folds .dockerignore into 2.2.
- **Manager queue at usage-limit pause (2026-09-05, evening):** merged since round 1: #64 storefront 1.3. **To do after reset, in this order:** (1) re-check #61 core 1.2–1.7 fix commit (guard: NODE_ENV=production refuses dev tokens before the CORE_DEV_TOKENS flag + test) → merge; (2) review + merge #69 infra #59 fix (Dockerfiles build workspace deps with `--filter <app>...`, ts-node in build stage pending REQUEST #60, dot-directory outputs copied, smoke per image; images job required again — verify it removed continue-on-error); (3) review + merge #65 auth 1.1 follow-up (#43: OTP CONDITIONAL, owner pre-enrolled, tests flipped; other windows must `pnpm dev --reset`); (4) review + merge #66 storefront 1.4 cart/checkout (design change accepted in principle: review step gated on customer input, payment session created at place-order; window 7 revisits for hosted fields — record for Phase 2); (5) review + merge #67 admin 1.5 screens + follow-ups (note its finding: constants exported from 'use client' modules become client refs — add to global gotchas). Decisions given: #60 (ts-node devDependency in apps/core) → accept, window 1; #68 (storefront next.config.ts → .mjs so `--prod` images boot) → accept, window 3; window 5 folds .dockerignore into 2.2; window 4 may start 1.6 locally while #67 is in review; window 2 cherry-picks 1.3 then 1.4 from auth/parked-13-14 after #65 merges.
- Manager rules in force: `pnpm-lock.yaml` is allowed on every branch (ownership check updated); after each merge the manager runs `pnpm install && pnpm test` on main and commits the lockfile; contract-change/request issues are decided by the manager, applied on main, then windows `git merge main`.
- Repo: https://github.com/mfx1590/Commerce-Platform · CI: `.github/workflows/ci.yml` (ownership, lint+typecheck+format, unit with Postgres, contract with Prism, preview placeholder) · Local stack: `pnpm dev`.
- Phase 0 commits on main: step 1 skeleton 99d4661/8681771 · step 2 domain 66c2ca6 · guide 5603a1b · step 3 ownership b23676f/434b401 · step 4 db 8c33bb7 · step 5 events 978a48a · step 6 contracts 42f59aa · step 7 ADRs 45f5d50 · steps 8–10 CI/compose/seeds 01b6174 · step 11 issues (GitHub #1–#30) + step 12 this commit.
- Delivered: docs/domain.md (44 entities) · packages/db (10 migrations incl. outbox, forced RLS, tenant client, 16 tests) · packages/events (24 schemas v1, types, validator) · packages/contracts (Store API 20 ops, Admin API 46 ops with x-permission, Prism mocks :4010/:4011, 15 tests) · ADRs 0001–0005 · docker-compose (Postgres 5433, Redis 6381, Redpanda 19092, Keycloak 8180, OpenFGA 8081, mocks) · seeds (3 brands × 200 products, 2 warehouses, 7 staff users, `SEED_IDS`) · 30 Phase 1 issues labelled window:core|auth|storefront|admin + phase:1.
- Decisions this phase: plain SQL migrations run by an in-repo runner over node-postgres (no ORM); Prism for mocks; Vitest; approved extra dev libs: ajv + ajv-formats, json-schema-to-typescript, openapi-typescript; docs/** excluded from prettier (tables are grepped by scripts).
- Known deviations from the window memory files (do not edit them; windows read this): Memory-2-auth references `docs/adr/auth.md` → real file is `docs/adr/0002-auth-model.md`; core "Seed script" and "RLS test" items are already delivered by packages/db (issues [core] 1.3 and 1.8 scope the remaining work).

## Phase 2 window plan (decided 2026-09-08; the manager creates the issues at Integration 1)
Budget rule: 2–4 build windows at once on Max 5x. Waves are dependency-ordered; a wave starts when the previous wave's blocking PRs are merged.
| Wave | Windows (worktree) | Why now | New worktree? |
|---|---|---|---|
| Int 1 | main (manager = integrator) in `../wt-integration` | wiring + contracts-v0.3; nothing else starts until tagged | yes |
| A | 1 core (cart/order state machine/inventory/returns) · 9 search (Algolia, Cloudinary, promotions) · 6 cms (Sanity schema, content routes) · 5 infra (2.5 observability, 2.6 Vault) | all four independent of each other; core is the base for wave B | 9, 6 new; 1, 5 reused |
| B | 7 payments (Stripe, tax, fraud, webhooks) · 8 shipping (EasyPost, labels, tracking) · 17 marketing (campaigns, feeds, segments, abandoned cart, admin section) · 4 admin (store view: orders/customers/promotions/settings against contracts) | need core's order/cart modules (7, 8) or contracts-v0.3 (17); admin builds against the mock | 7, 8, 17 new; 4 reused |
| C | 10 brands (brand A storefront) · 3 storefront (real-API polish, SEO, perf) | need cms + storefront + real Store API | 10 new; 3 reused |
| Int 2 | main | order → payment → ship → refund end to end; k6; brand 1 go-live | — |
Six new worktrees over Phase 2 (6, 7, 8, 9, 10, 17): `./scripts/new-window.sh <n> 2` each, start message `docs/start-messages/<nn>-<key>.md`. Models: 7 payments and 17 marketing on Fable (money, attribution); others Opus.

## Phase plan and gates
| Phase | Goal | Windows (in order, one at a time) | Gate (you verify) |
|---|---|---|---|
| 0 | Foundations & contracts | main | `pnpm dev` boots everything; contracts-v0.1 tagged |
| 1 | Isolated modules | 1 core ∥ 2 auth ∥ 3 storefront ∥ 4 admin ∥ 5 infra (parallel worktrees since 2026-09-04) | store admin cannot open Finance; one order in staging |
| Int 1 | wire it | main | first checkout end to end |
| 2 | Commerce complete, brand 1 live | 1 core → 7 payments → 8 shipping → 9 search → 4 admin → 6 cms → 5 infra → 10 brands(A) → 3 storefront → 17 marketing (parallel) | real test-mode order, refund, label; load test |
| Int 2 | launch brand 1 | main | go-live |
| 3 | Multi-store & HQ | 1 core → 11 warehouse → 4 admin → 2 auth → 12 data → 13 customers → 10 brands(B,C) | onboard a brand from UI; shared stock pool |
| Int 3 | join HQ & stores | main | — |
| 4 | Events, accounting, CRM | 14 events (alone, first) → 15 accounting → 16 engagement | ledger = PSP payouts to the cent |
| Int 4 | reconcile | main | — |
| 5 | Data platform & AI | 12 data | BI = ledger = PSP |
| Int 5 | validate | main | — |
| 6 | Scale & hardening | main | continuous |

## Window index
| # | Key | Title | First phase | Memory file | Model |
|---|---|---|---|---|---|
| 1 | core | Core commerce | 1 | docs/memory/Memory-1-core.md | Fable (owner decision 2026-09-04: budget not a constraint) |
| 2 | auth | Auth & RBAC | 1 | docs/memory/Memory-2-auth.md | Fable |
| 3 | storefront | Storefront starter & UI kit | 1 | docs/memory/Memory-3-storefront.md | Opus |
| 4 | admin | Admin application | 1 | docs/memory/Memory-4-admin.md | Opus |
| 5 | infra | Infra & DevOps | 2 | docs/memory/Memory-5-infra.md | Opus |
| 6 | cms | CMS & landing pages | 2 | docs/memory/Memory-6-cms.md | Sonnet |
| 7 | payments | Payments, tax, fraud | 2 | docs/memory/Memory-7-payments.md | Fable (manager decision 2026-09-08: money) |
| 8 | shipping | Shipping & fulfillment | 2 | docs/memory/Memory-8-shipping.md | Sonnet |
| 9 | search | Search, media, promotions | 2 | docs/memory/Memory-9-search.md | Sonnet |
| 10 | brands | Brand storefronts (A, B, C…) | 2 | docs/memory/Memory-10-brands.md | Sonnet |
| 11 | warehouse | Shared warehouse / WMS | 3 | docs/memory/Memory-11-warehouse.md | Sonnet |
| 12 | data | Data platform, BI & AI | 3 | docs/memory/Memory-12-data.md | Sonnet |
| 13 | customers | Customer accounts & identity | 3 | docs/memory/Memory-13-customers.md | Sonnet |
| 14 | events | Event bus & outbox relay | 4 | docs/memory/Memory-14-events.md | strongest |
| 15 | accounting | Automatic accounting | 4 | docs/memory/Memory-15-accounting.md | strongest |
| 16 | engagement | CRM, notifications & support | 4 | docs/memory/Memory-16-engagement.md | Sonnet |
| 17 | marketing | Marketing (campaigns, feeds, segments, attribution, referrals, reviews) | 2 | docs/memory/Memory-17-marketing.md | Fable (manager decision 2026-09-08: attribution) |

Start messages for every window: docs/start-messages/ · Ownership map (CI-enforced): docs/ownership.md

## Contract change log
- 2026-09-08 · CONTRACT CHANGE #162 (Admin API merchandising rules, window 9 task 2.2) + #180 (window 4: 401/403 undocumented) · accepted, branch `integration/contracts-0.4`: Admin API 0.4.0 — `/admin/stores/{storeId}/merchandising/rules[/{ruleId}]` + `…/merchandising/publish` (tag `search`; reads `store_staff`, writes/publish `store_admin` on `store:{storeId}`; 93 operations, was 87) and, for #180, `401` + `403` documented on EVERY admin operation (not only catalog; `getMe` 401 only) with a spec test that keeps it so; `CONTRACTS_VERSION` 0.4.0 · db 0.2.1 — `0130_merchandising_rule.sql` (the #162 SQL verbatim, RLS `store`, UNIQUE per store + scope) · search module drops `proposed/` and reads its permissions from the spec · no event topic (#162 open question: rules go to Algolia synchronously; nothing consumes them) · tag contracts-v0.4 after merge · producer: window 9 (router; window 1 mounts it, REQUEST in #162); consumers: window 4 (admin merchandising UI, Prism `Prefer: code=403` now works on any operation)
- 2026-09-08 · **contracts-v0.3** (Integration 1): Admin API 0.3.0 — `/admin/stores/{storeId}/marketing/**` (campaigns + launch/end, segments + preview/materialize, feeds + publish/items, referral-programs, referrals, reviews + moderate, reports/attribution, reports/promotions) and `/admin/marketing/{dashboard,segment-templates}` per docs/marketing-scope.md; Store API 0.3.0 — optional `currency` query on `listProducts`/`getProduct` (400 `validation_error` if the store does not sell it; window 3's finding); `CONTRACTS_VERSION` 0.3.0. Events 0.2.0 — v1 `campaign.launched`, `campaign.ended`, `feed.published`, `attribution.recorded` (own event; `order.placed` stays v1), `referral.converted`, `review.published`, `cart.abandoned` (envelope v1 enums widened in place, additive). db 0.2.0 — `0120_marketing.sql` (campaign, segment [store_nullable], segment_member, product_feed, attribution, referral_program, referral, review + RLS). Producers: window 1 (attribution at placement via `src/lib/attribution.ts`, `cart.abandoned` job), window 17 (the rest). Consumers: 17, 4, 12, 15, 16.
- 2026-09-04 · baseline contracts-v0.1 (Store API 0.1.0, Admin API 0.1.0, events v1, db migrations 0001–0009 + 0100)
- 2026-09-08 · CONTRACT CHANGE #100 (cart/order `metadata` exposed in the Store API, additive) · accepted, Store API 0.2.0 / CONTRACTS_VERSION 0.2.2 on main · window 3 deletes its local type extension; window 1 copies cart.metadata → order.metadata at placement (Integration 1)
- 2026-09-07 · CONTRACT CHANGE #77 (customers read = support, not viewer; PII minimisation) · accepted, Admin API 0.2.1 on main (permission tightening; consumers: window 4 nav gating for Customers, window 1 permission table reads the spec at runtime) · tag contracts-v0.2.1 later with v0.3
- 2026-09-05 · CONTRACT CHANGE #56 (admin list sort/order, additive) · accepted, Admin API 0.2.0 on main · tag contracts-v0.2 to be created by the owner after the current merge round · producer: window 1 follow-up task; consumer: window 4 [admin] 1.3

## Integration reports
### Phase 1 → Integration 1 (2026-09-08, branch `integration/phase1`, integrator = manager)
**Merge order:** nothing to merge — every Phase 1 branch and infra 2.1–2.4b were already on main. Int 1 was wiring + contracts-v0.3.
**What changed in contracts:** see the Contract change log (v0.3). Decisions: `attribution.recorded` is its own event; marketing reports use `viewer` on the store (the spec's "any relation" convention) so store staff and HQ analysts both read them; template writes `owner`, dashboard `analyst` on `organization:hq`.
**Wiring delivered:**
- Core auth: `KeycloakStaffTokenVerifier` (auth-sdk JWKS + hq-rbac scope middleware) is the default; `Bearer dev:<subject>` only with `CORE_DEV_TOKENS=1` outside production. `requirePermission` asks OpenFGA through auth-sdk `can()` for real tokens and the `role_assignment` stub for dev tokens; 503 fail-closed. hq-rbac mounted (`/admin/users*`, `/admin/audit-log`, `/admin/finance/ping`). Live suite `apps/core/test/auth-live.test.ts` (7 tests: real store-admin token → 200 `/admin/me` and products, exact 403 body on finance-gated ops, owner 200 `/admin/users`, invalid JWT 401, OpenFGA down 503) runs in the `auth-e2e` CI job and skips without the stack.
- Admin: `ADMIN_API_URL` (real core :9000) wins over `MOCK_ADMIN_API_URL`; Playwright and contract tests pin the mock.
- Attribution: `apps/core/src/lib/attribution.ts` + 8 tests; window 1 calls `recordAttribution` at placement (#104).
- Store API fallback proxy: explicit opt-in `CORE_STORE_API_FALLBACK=1` + `CORE_STORE_API_FALLBACK_URL` (same pattern as `CORE_DEV_TOKENS`); refused unconditionally in production before either variable is read, mount option refused too (tests in `apps/core/test/store-fallback.test.ts`). Unimplemented `/store/*` paths are proxied verbatim to Prism so the storefront runs on one base URL.
- `@platform/auth-sdk` gained the `default` export condition (the CommonJS core could not `require()` it under `pnpm dev`; tests and tsc never noticed).
**Checkout end to end — REAL: the core answered the catalogue (`GET /store`, categories, product list, product detail: 200 seeded products, EUR prices). MOCKED: the cart, checkout steps, payment session, order completion and the order confirmation all came from Prism's contract examples through the core's fallback proxy — no cart or order row exists in the database.** Detail: storefront production build with `STORE_API_URL=http://localhost:9000`, core with the fallback to :4010. Real from the core: `GET /store`, `/store/categories`, `/store/products` (200 seeded products, EUR prices from the default price list), `/store/products/alpine-backpack`. Proxied to Prism (core log `store api fallback →`): `POST /store/carts`, `POST …/line-items`, `GET …/carts/{id}` ×6, `POST …/payment-session` ×2, `POST …/complete`, `GET /store/orders/{id}` → "Thank you, order #1000 confirmed". PLP/PDP are real; cart, checkout and orders are the mock's examples until core 2.1/2.2 land. The Playwright checkout spec still targets the mock's fixture names (`Classic Tee`) and cannot run against the core yet — storefront 2.1 (#109) makes the journey data-independent.
**Surprises:** (1) hq-rbac was complete but never mounted — "framework-neutral" code has no call site until integration; name the mount point in acceptance criteria. (2) auth-sdk `requirePermission` returns a callable, the core's returns an Express handler; the adapter lives in the core. (3) The admin had no real-API switch at all (only `MOCK_ADMIN_API_URL`). (4) Window 5 tore the docker stack down mid-integration (2.5 observability overlay); the manager restored it with `pnpm dev`. (5) `db:medusa:migrate` prints ts-node type errors from `medusa-config.ts` after the module migrations succeed — harmless (schema `medusa` has 145 tables, the server boots) but noisy: window 1 follow-up.
**Gates on the branch:** `pnpm lint`, `typecheck` (17/17), `format:check`, `pnpm test` (17 packages green incl. core 16 files with the live suite), contracts `test:contract` 10 passed.
**What Phase 2 must fix first:** core 2.1 cart + 2.2 placement (unblocks the real checkout, storefront 2.1, admin 2.2); window 1 also owns the `medusa-config.ts` ts-node noise. Window 5: #99, and a CI variant running the storefront against the core once 2.2 lands.

## Global gotchas (things every window must know)
- **gitleaks scans every branch's full history** (CI checks out with fetch-depth 0, all refs): one secret-shaped test literal on ANY branch turns the `secret scan` job red on EVERY PR and on main at once (2026-09-08: window 7's `'placement-key-12345678'`-style idempotency keys in payments.test.ts blocked #174, #177, #183). Rule for windows: test keys and tokens must not look like secrets — use words (`placement-key-alpha`), never digit/hex tails; a false positive is fixed by the manager with an anchored allowlist entry in `infra/gitleaks.toml` (window 5's file, root-config exception), never by `continue-on-error`.
- Manager: a merge queue cannot be stopped from the harness once it is waiting on CI (killing the wrapper leaves `merge-queue.sh` running to completion). If a PR must not merge yet, do not start the queue; if it is already running, push a commit to the PR branch first (the queue's push is then rejected and it exits) — never rely on a stop.
- **Integration 1 (2026-09-08):** the core's Admin API takes real Keycloak staff tokens by default (OpenFGA-backed permissions); `CORE_DEV_TOKENS=1` keeps `Bearer dev:<subject>` for local work. `OPENFGA_STORE_ID`/`OPENFGA_MODEL_ID` come from `pnpm --filter @platform/auth-sdk fga:seed` (memory datastore: re-seed after an OpenFGA restart). Storefront against the core: `STORE_API_URL=http://localhost:9000` + `CORE_STORE_API_FALLBACK=1` + `CORE_STORE_API_FALLBACK_URL=http://localhost:4010` on the core. Admin against the core: `ADMIN_API_URL=http://localhost:9000`.
- Contracts are now **contracts-v0.3** (Store 0.3.0, Admin 0.3.0, events 0.2.0, db 0.2.0). `docs/domain.md` section 2b lists the marketing tables. A window that needs more files `CONTRACT CHANGE:` as before.
- A module with a "framework-neutral" API is not done until something mounts it: name the mount point in the acceptance criteria (hq-rbac lesson).
- Local ports (other projects own the defaults on this machine): Postgres 5433, Redis 6381, Keycloak 8180, OpenFGA 8081 (playground 3001), Redpanda 19092/18081, mocks 4010/4011. `.env.example` is the truth; `pnpm dev` copies it to `.env`.
- Apps connect as `platform_app` (`DATABASE_URL_APP`), never the owner URL. Raw `pool.query` returns zero rows by design: always use `createTenantClient` / `createOrganizationClient` from `@platform/db`.
- SQL: quote `"order"` and `"return"` (reserved words). `audit_log` and `stock_movement` are append-only for the app role.
- Contracts, events and db schema are frozen at contracts-v0.1: never edit `packages/contracts|events|db`; file `CONTRACT CHANGE:` and keep going against the mock. Generated files (`src/generated`) are committed; CI fails if stale.
- Prism docker image needs `--no-multiprocess`; Keycloak local persists in the `keycloak-data` volume (dev-file); realms import on first start only — after editing infra/keycloak/*.json run `pnpm dev --reset` or delete the volume. A restart takes ~15 s during which realms return 500: retry, do not add dev-login bypasses.
- Events carry no PII (`email_hash` only); money is integer minor units everywhere.
- **Branch protection is not available on this repo** (private + GitHub Free; the protection API returns 403 "Upgrade to GitHub Pro or make this repository public"). So "CI green before merge" and "never merge your own PR" are enforced by the manager's merge queue and the Reviewer step, not by the platform: a direct push to main would succeed. Windows must never push to main. Options if we want it enforced: GitHub Pro (~$4/month) or a public repo. Window 5's branch-protection docs describe the settings to apply the day it becomes available.
- Vitest + Next.js apps: the app tsconfig sets `jsx: preserve` for Next, so a test importing any component dies with "React is not defined" — set `esbuild: { jsx: 'automatic' }` in the package's vitest config (windows 3 and 4). An async server component is testable by calling it and walking the returned tree; no DOM needed.
- A PR whose branch conflicts with main gets **zero** CI checks, not failing ones (GitHub cannot build the merge ref) and nothing announces it. Merge main before asking for review.
- Customer tokens (customers realm) bind to a store by `store_code` claim stamped per storefront client, verified by `verifyCustomerToken(token, storeCode)`; the core resolves the code from the publishable key. `store.code` is unique only per organization — single-organization assumption until Phase 3 multi-org work revisits it.
- Shared docker stack etiquette (parallel windows): never run `pnpm dev --reset`, `docker compose down`, or recreate containers while other windows are active — a Keycloak recreate drops any realm state another window imported but has not merged yet. Change a realm with `node infra/keycloak/reimport.mjs <realm>`; tests create their own throwaway databases; if the stack must be restarted, the manager does it at a quiet moment.
- Next.js (windows 3, 4, 17): never export constants from a `'use client'` module and import them in a server component — they become client references (`undefined`/`b is not iterable`); keep constants in plain `*.config.ts` modules. Server actions crossing the boundary: use `.bind(null, …)`, not arrow wrappers. After `git merge main`, run `pnpm install` before judging typecheck failures (new workspace deps from other windows).
- Manager tooling: when resolving a branch's lockfile conflict, work on a detached HEAD from origin/<branch> and push `HEAD:refs/heads/<branch>`; never `git push origin <branch>` from the shared repo — the local ref belongs to the window's worktree and may hold unpushed commits (this pushed admin 1.2–1.4 into #42 on 2026-09-05).
- PR flow (decided 2026-09-04 after window 2 hit GitHub's one-open-PR-per-branch limit): one branch per window, one PR per task, the manager merges with a **merge commit** (never squash), the window keeps working on the same branch and opens the next PR, which then contains only the new task. No stacked branches.
- OpenFGA user object id = `staff_user.id` (uuid), not the Keycloak `sub` (window 2 decision, accepted): it survives an IdP migration and matches the `role_assignment` mirror key. The scope middleware resolves `sub` → `staff_user.id` first; seed tuples use `SEED_IDS.users`.

## Usage budget notes (Max 5x)
- Check /usage before big tasks. Past ~60% weekly by Wednesday → docs/tests only until reset.
